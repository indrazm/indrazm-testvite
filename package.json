{
  "name": "indrazm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others \"vite\" \"pnpx tailwindcss -i src/styles/style.css -o dist/main.css --watch\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "tailwindcss": "^3.3.2",
    "vite": "^4.3.9"
  }
}
