import Typed from "typed.js";

const typed = new Typed("#typed", {
  strings: ["creating products", "creating websites", "doing code"],
  typeSpeed: 100,
  backSpeed: 50,
  loop: true,
  smartBackspace: true,
});
